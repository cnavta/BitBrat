# .env.example
# BitBrat Platform - Environment Configuration Template
# Copy this file to .secure.local for local development.
# .secure.local is used by merge-env.js to generate .env.local

# --- Global Settings ---
# LOG_LEVEL=info # debug, info, warn, error

# --- Message Bus (NATS/PubSub) ---
# When running locally via npm run local, these are usually handled by global.yaml/infra.yaml
# MESSAGE_BUS_DRIVER=nats
# NATS_URL=nats://nats:4222
# BUS_PREFIX=local.

# --- Google Cloud / Firebase ---
# Required for all services to identify the project
GOOGLE_CLOUD_PROJECT=bitbrat-local
GCLOUD_PROJECT=bitbrat-local
FIREBASE_PROJECT_ID=bitbrat-local

# Path to your Google Application Credentials JSON file (MANDATORY).
# This MUST be an absolute path on your host machine.
# Docker bind mounts do not support ~ or relative paths in this context.
# Avoid spaces in the path.
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your/service-account-key.json

# --- OpenAI (Required for llm-bot) ---
OPENAI_API_KEY=your-openai-api-key

# --- Twitch Integration (Required for auth, ingress-egress, oauth-flow) ---
TWITCH_CLIENT_ID=your-twitch-client-id
TWITCH_CLIENT_SECRET=your-twitch-client-secret

# --- MCP (Model Context Protocol) ---
# Shared secret for authenticating between scheduler and MCP servers
MCP_AUTH_TOKEN=your-mcp-auth-token

# --- Discord Integration (Optional) ---
# DISCORD_BOT_TOKEN=your-discord-bot-token
# DISCORD_CLIENT_ID=your-discord-client-id
# DISCORD_CLIENT_SECRET=your-discord-client-secret

# --- Twilio Integration (Optional) ---
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token

# --- OBS Integration (Optional) ---
# OBS_WEBSOCKET_PASSWORD=your-obs-password
