# auth service local configuration
# Configure Twitch OAuth credentials in .secure.local
# See .secure.local.example for setup instructions
# TWITCH_CLIENT_ID and TWITCH_CLIENT_SECRET are sourced from .secure.local
# OAUTH_STATE_SECRET is sourced from .secure.local
