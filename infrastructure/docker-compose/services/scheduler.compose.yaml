services:
  scheduler:
    env_file:
      - .env.brat
    build:
      context: .
      dockerfile: Dockerfile.service
      args:
        SERVICE_NAME: scheduler
        SERVICE_ENTRY: dist/apps/scheduler-service.js
        SERVICE_PORT: "3000"
    environment:
      - MCP_AUTH_TOKEN=${MCP_AUTH_TOKEN}
    ports:
      - "${SCHEDULER_HOST_PORT:-3001}:${SERVICE_PORT:-3000}"
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      tool-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      bitbrat-network:
        aliases:
          - scheduler.bitbrat.local
