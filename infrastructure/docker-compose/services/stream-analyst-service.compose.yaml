services:
  stream-analyst-service:
    env_file:
      - .env.brat
    build:
      context: .
      dockerfile: Dockerfile.service
      args:
        SERVICE_NAME: stream-analyst-service
        SERVICE_ENTRY: dist/apps/stream-analyst-service.js
        SERVICE_PORT: "3010"
    ports:
      - "${STREAM_ANALYST_SERVICE_HOST_PORT:-8080}:${SERVICE_PORT:-8080}"
    environment:
      - NODE_ENV=local
      - BITBRAT_ENV=local
      - PORT=${SERVICE_PORT:-8080}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${SERVICE_PORT:-8080}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      nats:
        condition: service_healthy
      firebase-emulator:
        condition: service_healthy
    networks:
      bitbrat-network:
        aliases:
          - stream-analyst.bitbrat.local
