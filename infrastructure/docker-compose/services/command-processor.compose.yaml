services:
  command-processor:
    build:
      context: .
      dockerfile: Dockerfile.command-processor
    env_file:
      - .env.local
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/google-app-creds.json
    volumes:
      - ${GOOGLE_APPLICATION_CREDENTIALS:?Set GOOGLE_APPLICATION_CREDENTIALS to a local JSON file}:/var/secrets/google-app-creds.json:ro
    ports:
      - "${COMMAND_PROCESSOR_HOST_PORT:-3001}:${SERVICE_PORT:-3000}"
    networks:
      bitbrat-network:
        aliases:
          - command-processor.bitbrat.local
    depends_on:
      nats:
        condition: service_healthy
      firebase-emulator:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10

networks:
  bitbrat-network:
    external: true
