branch: feature/sprint-326-b8f1a2-integrated-version-handling
pushed: true
push_remote: "git@github.com:cnavta/BitBrat.git"
base: main
pr_url: null
status: pr_failed
compare_url: https://github.com/cnavta/BitBrat/pull/new/feature/sprint-326-b8f1a2-integrated-version-handling
attempt:
  at: "2026-06-26T20:27:00-04:00"
  methods_tried:
    - "gh pr create — UNAVAILABLE: GitHub CLI (`gh`) is not installed in this environment."
    - "GitHub REST API (POST /repos/cnavta/BitBrat/pulls) — BLOCKED: no GITHUB_TOKEN/GH_TOKEN credential available."
  error: "PR could not be created automatically: no `gh` CLI and no API token in the environment."
title: "Sprint 326 Deliverables – Integrated Version Handling"
body: |
  Generated by LLM agent per Sprint Protocol. Adds the integrated version-handling release tooling:
  `brat release <patch|minor|major|x.y.z>` / `npm run release[:dry]`. Single source of truth =
  architecture.yaml project.version; bumps architecture.yaml (only project.version, comment-preserving +
  re-validated — Law #2) + package.json + package-lock.json, rolls CHANGELOG [Unreleased] into a dated
  block, optional local git tag, --dry-run no-op. 32 new Jest tests; validate_deliverable.sh assertion +
  release:dry hook; docs in README/CONTRIBUTING/AGENTS.md §2.8.
notes: |
  Per Rule S13: the feature branch is pushed and the failed PR attempt is logged here. Sprint closure
  requires either a successfully created PR (record its URL above) or explicit owner acceptance of closure
  with this failure recorded. To open the PR manually, visit the compare_url above, or provide a
  GITHUB_TOKEN / install `gh` so the agent can create it.
