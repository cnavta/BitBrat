# Key Learnings – sprint-280-a1b2c3

- IDE "Move File" refactoring doesn't always catch string literals in `jest.mock()` or `jest.requireActual()`. Manual verification of these paths is necessary after moving test files.
- Sprint Protocol v2.5 worked well for isolating and fixing the reported issue with full traceability.
