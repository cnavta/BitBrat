# Key Learnings – sprint-193-d9a2b5

- The `brat` tool is central to the platform's lifecycle, managing everything from bootstrapping to decommissioning.
- Using `--` in `npm run` is crucial for passing flags to the underlying CLI tools.
- Keeping the CLI help updated is vital as it serves as the primary source for documentation generation.
