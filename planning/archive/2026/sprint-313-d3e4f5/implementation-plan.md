# Implementation Plan – sprint-313-d3e4f5

## Objective
- Fix the `brat setup` command to correctly set `routing.stage` in `RuleDoc` objects.
- Ensure `routing.stage` reflects the target stage of the routing slip (e.g., 'analysis' or 'reaction').
- Remove the `attributes: { stage: '...' }` from individual slip items in the initial rules.

## Scope
- `tools/brat/src/cli/setup.ts`
- `tools/brat/src/cli/setup.test.ts`

## Deliverables
- Corrected `getInitialRoutingRules` in `tools/brat/src/cli/setup.ts`.
- Updated assertions in `tools/brat/src/cli/setup.test.ts`.

## Acceptance Criteria
- Routing rules generated by `brat setup` have `routing.stage` set to the intended next stage.
- Slip items in these rules do not contain redundant stage attributes.
- Example structure for a reaction rule:
  ```json
  {
    "routing": {
      "stage": "reaction",
      "slip": [ { "id": "llm-bot", ... } ]
    }
  }
  ```

## Testing Strategy
- Update unit tests in `tools/brat/src/cli/setup.test.ts` to verify the exact structure of `routing.stage` and `routing.slip`.
- Run `npm test tools/brat/src/cli/setup.test.ts`.

## Definition of Done
- Code changes implemented.
- Tests pass.
- `validate_deliverable.sh` passes.
- PR created.
