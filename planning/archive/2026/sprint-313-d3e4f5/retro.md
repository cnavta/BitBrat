# Retro - sprint-313-d3e4f5

## What worked
- Quick identification of the schema mismatch based on user feedback.
- Unit tests now strictly enforce the expected data structure for routing rules.

## What didn't work
- Previous attempts at fixing the routing rules didn't fully capture the nuance of how `routing.stage` is used relative to individual slip items.

## Improvements for next time
- Consult existing examples of `RuleDoc` in the database or documentation more closely if there is ambiguity about property usage.
