branch: feature/sprint-329-bf2d2b-scheduler-full-event-topic
base: main
commit: e8cfb0b
pushed: true
pr_url: null
status: pr_attempt_failed_owner_accepted   # owner said "Sprint complete." (REQ-003), explicitly accepting this logged PR-attempt failure per Rule S13(b)
pr_attempt:
  method_attempted: [gh-cli, github-api]
  result: blocked
  reason: >
    No GitHub PR could be created automatically: the `gh` CLI is not installed in this environment
    and no GITHUB_TOKEN / GH_TOKEN is available for the GitHub API (Rule S13). The branch has been
    pushed successfully and is ready for a PR.
  create_pr_url: https://github.com/cnavta/BitBrat/pull/new/feature/sprint-329-bf2d2b-scheduler-full-event-topic
  suggested_title: "Sprint 329 Deliverables – scheduler emits full InternalEventV2 on a selectable topic"
  suggested_body: >
    Generated by LLM agent (Lead Implementor) per Sprint Protocol. Widens create_schedule's event to a
    full InternalEventV2 (egress incl. twitch, identity/candidates/qos/externalEvent/metadata/ingress
    overrides), adds an optional validated `topic` (curated allow-list internal.ingress.v1 /
    internal.egress.v1; default internal.ingress.v1), removes hard-coded egress/topic in
    executeSchedule/handleTick (system/ingress fallback only when unset), updates architecture.yaml
    scheduler publishes (+internal.egress.v1) and re-validates. OD-1/OD-2 Plan of Record; G4 BREAKING:
    no back-compat with the old partial event schema (existing data deleted). Full Jest suite green
    (1097 passed). Closes BL-329-001..201,500,501.
owner_action_required: >
  Either install `gh` / provide a GITHUB_TOKEN so the PR can be opened automatically, or open the PR
  manually via create_pr_url and record the URL here. Per Rule S13, sprint closure requires the PR URL
  recorded OR explicit owner acceptance of this logged PR-attempt failure.
